

// export const createContext = async({req, res}: Options) => {
//     const { session, user } = await getSession({ req , res });


// export type TContext = Awaited<ReturnType<typeof createContext>>;